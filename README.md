# contacts-list-using-copilot

## to run

```sh
npm install
```

```sh
npm run dev
```

### Currently only runs locally using `npm run dev`

- it will concurrently start `json-server` and the `react app` with vite

---
